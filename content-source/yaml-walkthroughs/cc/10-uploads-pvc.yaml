apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: uploads
  labels:
    app: felis
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 5Gi
