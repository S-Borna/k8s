apiVersion: v1
kind: Service
metadata:
  name: frontend
  labels:
    app: foreverhome
    component: frontend
spec:
  selector:
    app: foreverhome
    component: frontend
  ports:
    - port: 80
      targetPort: 5173
      protocol: TCP
  type: ClusterIP
